# Browser expansion matrix

No new support claim is made by this directory. Each future row requires the exact browser build, operating system, hardware class, runtime and model revision, storage behavior, offline result, extension API result, and test date.

Firefox and Safari do not support the Chromium extension surface. Mobile, private browsing, enterprise-managed profiles, and low-memory systems remain separate unknown environments until measured.

